#!/bin/bash
# Run the container in the current directory.

. ./shared/libs/container.sh

internalModelPath="/opt/stable-diffusion-webui/models"

checkModelPath
createCache

set -x
docker run --gpus all -p "$port:$port" -v "$modelPath:$internalModelPath" -v "$optDir:/opt" -v .:/docker "$containerName"
