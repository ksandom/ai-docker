#!/bin/bash
# Build the container in the current directory.

. ./shared/libs/container.sh

./update-thing
set -x
docker build -t "$containerName" .

./cmd "/inside-tools-buildTime/dump-external"
./cmd "/inside/inside-install"
