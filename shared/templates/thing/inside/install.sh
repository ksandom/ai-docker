#!/bin/bash
# Actions to be performed inside the container during the install. These will be persisted in the container that gets created.

# TODO Install any dependencies.
# TODO Install the app itself.
# TODO Any post-install abstractions.
